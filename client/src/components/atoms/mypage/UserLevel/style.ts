import styled from 'styled-components';

export const UserLevelWrapper = styled.div`
	.user-level-wrapper {
		display: flex;
		gap: 20px;
		justify-content: center;
		background-color: var(--main-color);
	}

	.user-level-image > div {
		font-size: 36px;
	}

	.user-level-content > p {
		font-size: 36px;
	}
`;
