import styled from 'styled-components';

export const NavBarContainer = styled.div`
	height: 100%;
	display: flex;
	flex-direction: row;
	align-items: center;

	.logo {
		display: flex;
		align-items: center;

		svg {
			width: 170px;
			height: 56px;
		}
	}

	.search-bar {
	}
`;
