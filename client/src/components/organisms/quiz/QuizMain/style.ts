import styled from 'styled-components';

export const QuizMainContainer = styled.div`
	margin-top: 3rem;
	.quiz-button {
		display: flex;
		justify-content: center;
		align-item: center;
		margin-top: 3rem;
	}
`;
