import styled from 'styled-components';

export const QuizWordsWrapper = styled.div`
	.checkbox-wrapper {
		margin-top: 3rem;
		display: grid;
		justify-content: center;
		align-item: center;
	}

	.subtitle {
		margin-top: 3rem;
		font-size: 2rem;
		display: flex;
		justify-content: center;
		align-item: center;
	}

	.quiz-button-wrapper {
		margin-top: 3rem;
		display: flex;
		justify-content: center;
		align-item: center;
	}
`;
