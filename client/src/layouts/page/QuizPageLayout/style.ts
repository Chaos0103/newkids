import styled from 'styled-components';

export const QuizPageLayoutContainer = styled.div`
	.quiz-button {
		display: flex;
		justify-content: center;
		align-item: center;
		margin-top: 3rem;
	}
`;
